import { styled } from '@material-ui/core/styles';
import MyButton from '../../../adapters/Button';
import MyDialogTitle from '../../../adapters/MyDialogTitle/index';

export const SubmitButton = styled(MyButton)({
  backgroundColor: '#eee'
});

export const StyledDialogTitle = styled(MyDialogTitle)({
  backgroundColor: '#EEEEEE'
});
